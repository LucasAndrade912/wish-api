services:
    db:
        container_name: wish-database
        image: postgres
        environment:
            - POSTGRES_USER=admin
            - POSTGRES_PASSWORD=adminpass
            - POSTGRES_DB=wish
        ports:
            - "5432:5432"
        volumes:
            - db_data:/var/lib/postgresql/data

volumes:
    db_data:
